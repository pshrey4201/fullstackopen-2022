{
  "name": "bloglist",
  "version": "1.0.0",
  "description": "A program that allows users to save information about interesting blogs they have stumbled across on the internet.",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node index.js",
    "dev": "NODE_ENV=development nodemon index.js",
    "test": "NODE_ENV=test jest --verbose --runInBand --forceExit",
    "lint": "eslint ."
  },
  "author": "Shrey Patel",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.23.1",
    "jest": "^29.3.1",
    "nodemon": "^2.0.20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.2",
    "express": "^4.18.1",
    "mongoose": "^6.6.1",
    "morgan": "^1.10.0"
  },
  "jest": {
    "testEnvironment": "node"
  } 
}
